-   id: tslint
    name: tslint
    description: Runs tslint over typescript source
    entry: tslint
    language: node
    files: \.ts$

    # Use additional_dependencies to install the actual node package
    additional_dependencies: ['tslint@3.15.1', 'typescript']
    minimum_pre_commit_version: 0.7.0
